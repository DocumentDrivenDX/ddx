#!/bin/sh
log="${DDX_PINNED_PROVIDER_GUARD_LOG:-/dev/null}"
printf '{"blocked_harness":"gemini","requested_harness":"codex","argv":"%s","pwd":"%s"}\n' "$*" "$(pwd)" >> "$log" 2>/dev/null || true
echo "ddx: blocked unrequested provider harness "gemini" during pinned "codex" execution" >&2
exit 126
