The deep code trace confirms my thesis and pinpoints the actual bug far more precisely than the design does. Two refinements to my findings — one correction, one sharpening:

## Sharpened findings (post-trace)

**The suppression bug is exact and findable — and the design would fix the wrong layer.** Incident #1 ("stale terminal state suppressed restart") is `canStartMore` returning `false` whenever `len(s.blockedTerminals) > 0` (`workers_supervisor.go:416-418`) — *any* blocked terminal suppresses *all* restarts for the project. The block is only cleared by `clearResolvedTerminalBlocksLocked` (`441-453`), which deletes it **only if `desired.updated_at > block.TerminalAt`** (line 449). So a terminal row whose `TerminalAt` (`workerTerminalTime`, 501-509) is newer than the last `desired.json` write persists forever and keeps restart suppressed until an operator rewrites `desired.json`. That is precisely the observed "stopped being server-managed until manually fixed." **The permanent fix is a time/liveness-based expiry (or generation counter) on `blockedTerminals`, not a new slot store** — proposal #1 rebuilds the wrong layer and leaves this bug in place.

**Correction to my PID-only finding:** `WorkerRecord` *does* persist `PGID` (`workers.go:129`) and there's a full process-group reaper (`workers_reap_unix.go`). The real gap is narrower: the *reconcile liveness predicate* still decides with `processAlive(rec.PID)` (PID only, `workers_supervisor.go:319`) and ignores the persisted PGID/session. So the design's "reconcile against PID/PGID" is a predicate change in one function, not a schema migration — smaller than I stated, but still unspecified.

**Readiness decode — behavior now confirmed:** the acceptance-array bug hits `decodeCanonicalReadinessPayloadWithMode`, which returns a hard error on `json.Unmarshal` (`preclaim_intake_hook.go:770-773`) → `intakeErr` → the bead is **parked** via `parkBeadPostIntakeRejection` (`execute_bead_loop.go:2829`), not crashed. Note this is a *different* path from `newPreClaimIntakeHook`'s fail-open dispatch-error swallow (`:496-499`). So `readiness_decode_unavailable` must state which of these two paths it governs — the design currently conflates decode-error (fail-closed→park) with dispatch-error (fail-open→proceed).

**Concurrency — confirmed and asymmetric:** run-state is safe (atomic temp+rename, `run_state.go:261-291`), but the append JSONL streams (`executions_mirror.go:339`, `worker_ingest.go:217`, `lockmetrics.go:120`, routing/attempt metrics) are guarded by **per-process `sync.Mutex` only — no cross-process `flock`**. Managed-launch subprocess workers therefore *can* interleave writes. The "JSONL conflict during concurrent sync" incident is real and the design must mandate flock or a single-writer owner for these specific files.

**Multi-project meta-scan already half-exists:** `SupervisorRegistry.ReconcileAll` iterates projects with per-project error isolation (`workers_supervisor_registry.go:107-125`), and `seedSupervisedProjects` discovers them via `DDX_SUPERVISED_PROJECTS` + subdir scan for `workers/desired.json` (`155-211`). Proposal #3 must **extend** this, not invent a parallel scanner — and "configured projects" already has a concrete source of truth (`ServerState.Projects`, `state.go:134-137,377-410`).

## Verdict: **BLOCK** (unchanged)

The trace strengthens, not weakens, the block. The design is greenfield-framed over a live supervisor (`workers_supervisor.go`, `workers_supervisor_registry.go`) that already has desired-count reconcile, restart backoff, per-project meta-scan, PGID reaping, and atomic run-state. The single highest-value fix — expiring `blockedTerminals` so a stale terminal can't suppress restart forever (`:416-418,441-453`) — is a ~10-line change the design would bury under a from-scratch slot store. Before decomposition: (1) resolve extend-vs-replace against the existing supervisor and re-anchor the suppression fix on `blockedTerminals`/`clearResolvedTerminalBlocksLocked`; (2) split readiness decode-error vs dispatch-error handling; (3) mandate flock/single-writer for the named append-JSONL files; (4) carve beads with these exact files and `Test*` targets.

Want me to (a) write this to `self-healing-workers-findings-claude-r1.md` (needs write approval), or (b) draft the corrected bead breakdown anchored on the existing supervisor?