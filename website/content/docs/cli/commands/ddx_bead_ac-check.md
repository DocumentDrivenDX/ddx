---
title: "bead ac-check"
generated: true
---

## ddx bead ac-check

Mechanically verify acceptance criteria against the working tree

### Synopsis

ac-check classifies each AC into a kind (test-name | build-gate |
negative | symbol | mechanical | prose) and gathers evidence
mechanically. Output is emitted as JSON to stdout (when --json) and to
.ddx/executions/<attempt-id>/ac-check.json when --attempt-id is given.

Examples:
  ddx bead ac-check ddx-abc123
  ddx bead ac-check ddx-abc123 --rev-range main..HEAD --json
  ddx bead ac-check ddx-abc123 --attempt-id 20260510T210000-abcdef00

```
ddx bead ac-check <bead-id> [flags]
```

### Options

```
      --attempt-id string   if set, persist ac-check.json under .ddx/executions/<attempt-id>/
  -h, --help                help for ac-check
      --json                emit JSON to stdout instead of the human-readable table
      --package strings     Go packages to scope test runs to (default ./...)
      --rev-range string    git diff range used to scope DiffHits (e.g., main..HEAD)
```

### Options inherited from parent commands

```
      --config string              config file (default is $HOME/.ddx.yml)
      --library-base-path string   override path for DDx library location
  -v, --verbose                    verbose output
```

### SEE ALSO

* [ddx bead](/docs/cli/commands/ddx_bead/)	 - Manage beads (portable work items)

###### Auto generated by spf13/cobra on 12-May-2026
