---
title: "server state prune"
generated: true
---

## ddx server state prune

Remove phantom test-directory project entries from server-state.json

### Synopsis

Prune drops project entries whose path looks like a Go test temp
directory (any path under /tmp/, /private/tmp/, /var/folders/, or any path
containing a /TestXxx<digits>/ segment). Before overwriting the state file,
the existing content is written to a timestamped ".bak-YYYYMMDDTHHMMSS"
sibling so the operation is recoverable.

Use --dry-run to preview the summary without modifying anything.

```
ddx server state prune [flags]
```

### Options

```
      --dry-run        Print the summary without modifying the state file
  -h, --help           help for prune
      --state string   Path to server-state.json (default: XDG_DATA_HOME/ddx/server-state.json)
```

### Options inherited from parent commands

```
      --config string              config file (default is $HOME/.ddx.yml)
      --library-base-path string   override path for DDx library location
  -v, --verbose                    verbose output
```

### SEE ALSO

* [ddx server state](/docs/cli/commands/ddx_server_state/)	 - Inspect and repair the ddx-server persistent state file

###### Auto generated by spf13/cobra on 12-May-2026
