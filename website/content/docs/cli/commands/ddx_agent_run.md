---
title: "agent run"
generated: true
---

## ddx agent run

Removed compatibility surface.

Use `ddx run` for one direct agent invocation. Use `ddx try` for one bead
attempt and `ddx work` to drain ready queue work. For side-by-side prompt or
harness comparison, use the `compare-prompts` skill instead of this removed
command family.

DDx may pass explicit harness, provider, or model values through to the agent
when the operator supplies them. Those values are passthrough debug controls,
not DDx routing policy.

### Options inherited from parent commands

```
      --config string              config file (default is $HOME/.ddx.yml)
      --library-base-path string   override path for DDx library location
  -v, --verbose                    verbose output
```

### SEE ALSO

* [ddx agent](/docs/cli/commands/ddx_agent/)	 - Agent diagnostics and legacy command namespace

###### Auto generated by spf13/cobra on 21-Apr-2026
