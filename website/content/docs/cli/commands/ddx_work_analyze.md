---
title: "work analyze"
generated: true
---

## ddx work analyze

Query cross-attempt performance from .ddx/metrics/attempts.jsonl

### Synopsis

Aggregate and analyze attempt data from .ddx/metrics/attempts.jsonl.

Group rows by one or more dimensions (--by) and compute aggregated metrics.
Filter with --since (time window) or --where (field equality).

Supported dimensions: model, harness, profile, prompt_template_hash, spec_id,
outcome, bead_id, day, week, month.

Examples:
  ddx work analyze --by model
  ddx work analyze --by model,outcome
  ddx work analyze --by spec_id --since 7d
  ddx work analyze --where model=claude-sonnet-4-6 --by prompt_template_hash

```
ddx work analyze [flags]
```

### Options

```
      --by strings          Dimensions to group by (comma-separated): model, harness, profile, prompt_template_hash, spec_id, outcome, bead_id, day, week, month, reviewer_model
      --csv                 Emit CSV output
  -h, --help                help for analyze
      --json                Emit JSONL output
      --project string      Target project root (default: CWD git root)
      --since string        Time window: e.g. 7d, 2w, 1m (days/weeks/months)
      --where stringArray   Filter by key=value; may be repeated
```

### Options inherited from parent commands

```
      --config string              config file (default is $HOME/.ddx.yml)
      --library-base-path string   override path for DDx library location
  -v, --verbose                    verbose output
```

### SEE ALSO

* [ddx work](/docs/cli/commands/ddx_work/)	 - Drain the bead execution queue

###### Auto generated by spf13/cobra on 12-May-2026
