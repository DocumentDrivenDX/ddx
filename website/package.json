{
  "name": "ddx-website-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "hugo server --bind 0.0.0.0 --port 1313 --disableFastRender",
    "test:e2e": "playwright test",
    "test:screenshots": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
